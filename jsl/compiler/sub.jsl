; The first frontend semantic boundary. TypeScript proves raw integer inputs around this call, but
; JavaScript subtraction still enters through a dynamic JSL function before specialization.
(builtin JsSub :params [(a dyn) (b dyn)] :ret dyn
  (%Box (%Sub (%UnboxInt a) (%UnboxInt b))))
