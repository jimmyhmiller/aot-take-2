; JavaScript addition enters through JSL. The current typed subset proves integer operands; the
; dynamic boundary remains explicit so string concatenation can extend this definition later.
(builtin JsAdd :params [(a dyn) (b dyn)] :ret dyn
  (%Box (%Add (%UnboxInt a) (%UnboxInt b))))
