; Declared after JsChoose deliberately: the production index must resolve forward semantic calls
; through its declaration pass before lowering either body.
(builtin JsIdentity :params [(value dyn)] :ret dyn value)
