;; Graphviz output. Emitting a format is not authoring in it — see CLAUDE.md's second rule.

;;
;; SCAFFOLD: signatures are final; bodies hard-error. Filled in at S1.
(module aot.print.dot)
(import "aot.util.panic" :use *)
(import "aot.node.node" :use *)

(defn dot-graph [(root (dyn NodeOps))] (-> (slice u8))
  (do (unimplemented "dot-graph" "S1 — src/print/dot.coil") ""))
