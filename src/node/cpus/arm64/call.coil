;; arm64 machine nodes: the call family.

;;
;; SCAFFOLD: filled in at S6. See src/node/cpus/arm64/arm64.coil.
(module aot.node.cpus.arm64.call)
(import "aot.util.panic" :use *)
(import "aot.node.node" :use *)

(defn arm64-call-select [(n (dyn NodeOps))] (-> (dyn NodeOps))
  (do (unimplemented "arm64-call-select" "S6 — src/node/cpus/arm64/call.coil") (unreachable-node)))
