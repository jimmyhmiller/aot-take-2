;; arm64 machine nodes: the bits family.

;;
;; SCAFFOLD: filled in at S6. See src/node/cpus/arm64/arm64.coil.
(module aot.node.cpus.arm64.bits)
(import "aot.util.panic" :use *)
(import "aot.node.node" :use *)

(defn arm64-bits-select [(n (dyn NodeOps))] (-> (dyn NodeOps))
  (do (unimplemented "arm64-bits-select" "S6 — src/node/cpus/arm64/bits.coil") (unreachable-node)))
