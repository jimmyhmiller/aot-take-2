;; arm64 machine nodes: the branch family.

;;
;; SCAFFOLD: filled in at S6. See src/node/cpus/arm64/arm64.coil.
(module aot.node.cpus.arm64.branch)
(import "aot.util.panic" :use *)
(import "aot.node.node" :use *)

(defn arm64-branch-select [(n (dyn NodeOps))] (-> (dyn NodeOps))
  (do (unimplemented "arm64-branch-select" "S6 — src/node/cpus/arm64/branch.coil") (unreachable-node)))
