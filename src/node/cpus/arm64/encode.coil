;; arm64 machine nodes: the encode family.

;;
;; SCAFFOLD: filled in at S6. See src/node/cpus/arm64/arm64.coil.
(module aot.node.cpus.arm64.encode)
(import "aot.util.panic" :use *)
(import "aot.node.node" :use *)

(defn arm64-encode-select [(n (dyn NodeOps))] (-> (dyn NodeOps))
  (do (unimplemented "arm64-encode-select" "S6 — src/node/cpus/arm64/encode.coil") (unreachable-node)))
