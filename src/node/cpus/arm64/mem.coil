;; arm64 machine nodes: the mem family.

;;
;; SCAFFOLD: filled in at S6. See src/node/cpus/arm64/arm64.coil.
(module aot.node.cpus.arm64.mem)
(import "aot.util.panic" :use *)
(import "aot.node.node" :use *)

(defn arm64-mem-select [(n (dyn NodeOps))] (-> (dyn NodeOps))
  (do (unimplemented "arm64-mem-select" "S6 — src/node/cpus/arm64/mem.coil") (unreachable-node)))
