;; arm64 machine nodes: the arith family.

;;
;; SCAFFOLD: filled in at S6. See src/node/cpus/arm64/arm64.coil.
(module aot.node.cpus.arm64.arith)
(import "aot.util.panic" :use *)
(import "aot.node.node" :use *)

(defn arm64-arith-select [(n (dyn NodeOps))] (-> (dyn NodeOps))
  (do (unimplemented "arm64-arith-select" "S6 — src/node/cpus/arm64/arith.coil") (unreachable-node)))
