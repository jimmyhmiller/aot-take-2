;; x86-64 machine nodes: the mem family.

;;
;; SCAFFOLD: filled in at S6. See src/node/cpus/x86_64/x86_64.coil.
(module aot.node.cpus.x86-64.mem)
(import "aot.util.panic" :use *)
(import "aot.node.node" :use *)

(defn x86-mem-select [(n (dyn NodeOps))] (-> (dyn NodeOps))
  (do (unimplemented "x86-mem-select" "S6 — src/node/cpus/x86_64/mem.coil") (unreachable-node)))
