;; x86-64 machine nodes: the encode family.

;;
;; SCAFFOLD: filled in at S6. See src/node/cpus/x86_64/x86_64.coil.
(module aot.node.cpus.x86-64.encode)
(import "aot.util.panic" :use *)
(import "aot.node.node" :use *)

(defn x86-encode-select [(n (dyn NodeOps))] (-> (dyn NodeOps))
  (do (unimplemented "x86-encode-select" "S6 — src/node/cpus/x86_64/encode.coil") (unreachable-node)))
