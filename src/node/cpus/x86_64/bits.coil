;; x86-64 machine nodes: the bits family.

;;
;; SCAFFOLD: filled in at S6. See src/node/cpus/x86_64/x86_64.coil.
(module aot.node.cpus.x86-64.bits)
(import "aot.util.panic" :use *)
(import "aot.node.node" :use *)

(defn x86-bits-select [(n (dyn NodeOps))] (-> (dyn NodeOps))
  (do (unimplemented "x86-bits-select" "S6 — src/node/cpus/x86_64/bits.coil") (unreachable-node)))
